const userReducer = (state= {}, action) => {
    switch(action.type) {
        case "AUTHENTICATE_USER":
            return state
        default:
            return state
    }


}

export default userReducer;